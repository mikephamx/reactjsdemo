import React from 'react' //RFC create functional component

export default function HeaderRFC() { //it's only a function, doesn't contain any attribute or methods, different from class
  return (
    <div>
       <h3>React Header component</h3> 
    </div>
  )
}
